# Face-Mask-Detection-System
Face Mask Detection System is a real-time computer vision application built using Python and Jupyter Notebook. It uses Deep Learning (CNN) and OpenCV to detect faces from live webcam video. The system classifies each detected face as Mask or No Mask in real time.
